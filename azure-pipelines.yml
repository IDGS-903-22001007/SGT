# =====================================================
# 📦 Pipeline CI/CD para App Flask (SGT)
# Flujo: CI (Empaquetado) -> CD (Deploy a Azure WebApp)
# =====================================================

trigger:
- main

pool:
  name: 'AgenteLocal'

variables:
  # AppSourceDir: '$(Build.SourcesDirectory)/sgt' # Esta variable ya no se usa, pero la mantengo comentada
  ArtifactName: 'sgt-deploy-package'
  DeploymentPath: '$(Build.ArtifactStagingDirectory)/$(ArtifactName)'
  ServiceConnectionName: 'AZ-Conexion-SGT'
  AppServiceName: 'sgt-app-integrated-2025'
  PythonVersion: '3.11' 

# =====================================================
# 1️⃣ Etapa: CI - Integración Continua (Empaquetado)
# =====================================================
stages:
- stage: CI_Build
  displayName: 'Etapa 1: Integración Continua (Empaquetado)'
  jobs:
  - job: PythonCI
    displayName: 'Job: Instalación y Empaquetado'
    pool:
      # Asegura que el job use el pool
      name: 'AgenteLocal' 
    steps:

    # --- 0. Limpiar workspace ---
    - script: |
        echo "🧹 Limpiando directorio de trabajo previo..."
        git clean -xfd
      displayName: 'Limpiar workspace'

    # --- 1. Configurar versión de Python ---
    - task: UsePythonVersion@0
      displayName: 'Configurar Python $(PythonVersion)'
      inputs:
        versionSpec: '$(PythonVersion)'

    # --- 2. Instalar dependencias ---
    - script: |
        echo "📦 Instalando dependencias..."
        pip install --upgrade pip
        pip install -r requirements.txt || (echo "Reintentando instalación..." && pip install -r requirements.txt)
      displayName: 'Instalar Flask y dependencias'

    # --- 3. Copiar archivos para despliegue ---
    - task: CopyFiles@2
      displayName: 'Copiar Código de Aplicación'
      inputs:
        SourceFolder: '$(Build.SourcesDirectory)'
        Contents: |
          sgt/** # Código fuente
          requirements.txt     # Dependencias
          app.py               # Archivo Python en la raíz (si existe)
          templates/** # ✅ CRÍTICO: Templates en la raíz
          reportes/** # ✅ AGREGADO: Carpeta de reportes (si existe)
          
          # Si tienes main.py en la raíz, agrégalo aquí:
          # main.py
        # El directorio de destino se crea si es necesario.
        TargetFolder: '$(DeploymentPath)'

    # --- 4. Publicar artefactos ---
    - task: PublishBuildArtifacts@1
      displayName: 'Publicar Artefacto de Despliegue'
      inputs:
        pathToPublish: '$(DeploymentPath)'
        artifactName: '$(ArtifactName)'

# =====================================================
# 2️⃣ Etapa: CD - Despliegue Continuo en Azure
# =====================================================
- stage: CD_Deployment
  displayName: 'Etapa 2: Despliegue en Azure App Service'
  dependsOn: CI_Build
  condition: succeeded()
  
  jobs:
  - deployment: DeployToAzure
    displayName: 'Job: Desplegar Flask App en Azure'
    environment: 'Entorno-Integrado-SGT'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: DownloadPipelineArtifact@2
            displayName: 'Descargar Artefacto de Código Empaquetado'
            inputs:
              artifactName: '$(ArtifactName)'
              path: '$(Pipeline.Workspace)/$(ArtifactName)'

          - task: AzureWebApp@1
            displayName: 'Desplegar Flask App a Azure App Service'
            inputs:
              azureSubscription: '$(ServiceConnectionName)'
              appName: '$(AppServiceName)'
              appType: webApp
              package: '$(Pipeline.Workspace)/$(ArtifactName)/'
              startupCommand: 'gunicorn --bind=0.0.0.0 --workers=4 --timeout=90 sgt.main:app'

# =====================================================
# 3️⃣ Etapa: Test Funcional Post-Deploy (Opcional)
# =====================================================
- stage: Functional_Tests
  displayName: "Etapa 3: Verificacion Post Despliegue (opcional)"
  dependsOn: CD_Deployment
  condition: succeeded()
  
  jobs:
  - job: FunctionalTest
    displayName: "Job: Verificacion del Despliegue (externa)"
    pool:
      name: "AgenteLocal"
    steps:
      - script: |
          echo "Despliegue completado correctamente."
          echo "La verificacion funcional sera realizada con una herramienta externa."
        displayName: "Notificacion de exito"
